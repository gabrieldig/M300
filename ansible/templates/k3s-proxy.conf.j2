# ==========================================
# AUTOMATISCHES PORT FORWARDING INS K3S CLUSTER
# ==========================================

# 1. Grafana Dashboard
server {
    listen 30080;
    server_name _;

    location / {
        proxy_pass http://{{ target_master_ip }}:30080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # Wichtig für WebSockets (Live-Aktualisierung in Dashboards)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# 2. Prometheus Monitoring
server {
    listen 30090;
    server_name _;

    location / {
        proxy_pass http://{{ target_master_ip }}:30090;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# 3. Headlamp Kubernetes UI
server {
    listen 30100;
    server_name _;

    location / {
        proxy_pass http://{{ target_master_ip }}:30100;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# 4. M300 Task Manager App (Frontend)
server {
    listen 30200;
    server_name _;

    location / {
        proxy_pass http://{{ target_master_ip }}:30200;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}